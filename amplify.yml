version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - cd miracle-messages
        - yarn install
    build:
      commands:
        - yarn run build
  artifacts:
    baseDirectory: miracle-messages/build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
